import{_ as a,o as t,c as o,ao as i}from"./chunks/framework.fV223pj9.js";const m=JSON.parse('{"title":"Phase transition and frozen soil","description":"","frontmatter":{},"headers":[],"relativePath":"manual/phase_transition.md","filePath":"manual/phase_transition.md","lastUpdated":null}'),r={name:"manual/phase_transition.md"};function n(s,e,c,l,h,d){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Phase-transition-and-frozen-soil" tabindex="-1">Phase transition and frozen soil <a class="header-anchor" href="#Phase-transition-and-frozen-soil" aria-label="Permalink to &quot;Phase transition and frozen soil {#Phase-transition-and-frozen-soil}&quot;">​</a></h1><p>Water freezing and thawing is handled by two separate mechanisms in two separate parts of the model — an hourly correction for the <em>soil</em> column, and a continuous one inside the <em>snow</em> column&#39;s own heat ODE — plus a downstream effect on soil hydraulics: once a soil layer is frozen, water is treated as unable to flow through it, either through the soil matrix or through roots.</p><h2 id="The-hourly-latent-heat-correction-soil" tabindex="-1">The hourly latent-heat correction (soil) <a class="header-anchor" href="#The-hourly-latent-heat-correction-soil" aria-label="Permalink to &quot;The hourly latent-heat correction (soil) {#The-hourly-latent-heat-correction-soil}&quot;">​</a></h2><p><a href="/Microclimate.jl/previews/PR130/api#Microclimate.PhaseTransitionLatentHeat"><code>PhaseTransitionLatentHeat</code></a> is soil&#39;s only freeze/thaw mechanism: the <a href="./soil_thermal_properties">soil heat ODE</a> and its <a href="/Microclimate.jl/previews/PR130/api#Microclimate.CampbelldeVriesSoilProperties"><code>CampbelldeVriesSoilProperties</code></a> thermal properties have no phase-transition term of their own. Instead, <code>PhaseTransitionLatentHeat</code> applies a discrete correction once per hour, after the ODE has advanced: it detects whether the mean temperature of each pair of adjacent nodes has crossed 0°C between the previous and current hour, and if so, accumulates (on freezing) or depletes (on thawing) a per-layer latent-heat budget against <code>mass × L_fusion</code>. While that budget is non-empty, the layer&#39;s temperature is clamped to exactly 0°C; once it&#39;s exhausted, the clamp releases and the layer cools or warms freely again. This closely follows NicheMapR Fortran&#39;s <code>OSUB.f:571-602</code>, and fires only on an actual crossing — a layer that&#39;s already clamped to 0°C stays there without re-triggering the branch.</p><p><code>frozen_water_content!</code> converts each layer&#39;s accumulated latent-heat budget into a frozen fraction of that layer&#39;s <code>soil_moisture</code> (<code>accumulated_latent_heat / (mass × L_fusion)</code>, clamped to <code>[0, 1]</code>) — the quantity the soil hydraulics below actually consumes.</p><h2 id="Apparent-heat-capacity,-inside-the-ODE-snow" tabindex="-1">Apparent heat capacity, inside the ODE (snow) <a class="header-anchor" href="#Apparent-heat-capacity,-inside-the-ODE-snow" aria-label="Permalink to &quot;Apparent heat capacity, inside the ODE (snow) {#Apparent-heat-capacity,-inside-the-ODE-snow}&quot;">​</a></h2><p>Snow, unlike soil, is solved with an <em>apparent</em> specific heat capacity, boosted near 0°C to absorb the latent heat of fusion as <a href="/Microclimate.jl/previews/PR130/api#Microclimate.SnowModel"><code>SnowModel</code></a>&#39;s own heat ODE integrates through the freezing point, rather than treating it as a separate hourly event. This is a distinct mechanism from soil&#39;s hourly correction above, plugged in via <code>SnowModel</code>&#39;s <code>apparent_heat_capacity</code> field (default <a href="/Microclimate.jl/previews/PR130/api#Microclimate.BonacinaStep"><code>BonacinaStep</code></a>). Four interchangeable forms are available:</p><ul><li><p><a href="/Microclimate.jl/previews/PR130/api#Microclimate.BonacinaStep"><code>BonacinaStep</code></a> — a step function over a fixed band (-0.45°C to 0.4°C by default), reproducing NicheMapR Fortran&#39;s <code>SOILPROPS.f</code> exactly. Its discontinuity is hard on adaptive ODE solvers: <code>Tsit5</code> tends to pin the temperature at the band edge rather than cross it.</p></li><li><p><a href="/Microclimate.jl/previews/PR130/api#Microclimate.TanhSmoothed"><code>TanhSmoothed</code></a> — the same step, replaced by a pair of <code>tanh</code> ramps at the band edges, so the solver sees a C∞-smooth <code>cp</code> and can cross the band in normal steps, while matching <code>BonacinaStep</code>&#39;s integrated latent budget closely.</p></li><li><p><a href="/Microclimate.jl/previews/PR130/api#Microclimate.Gaussian"><code>Gaussian</code></a> — a normal-distribution bump centred at the melting point, whose integral equals the latent heat of fusion exactly.</p></li><li><p><a href="/Microclimate.jl/previews/PR130/api#Microclimate.WestermannSigmoid"><code>WestermannSigmoid</code></a> — derives <code>cp</code> from the derivative of an assumed unfrozen-water-fraction sigmoid (the CryoGrid formulation; Westermann et al. 2011, 2016), so the shape emerges from a physical liquid-fraction model rather than being imposed directly.</p></li></ul><h2 id="Frozen-soil-blocks-water-transport" tabindex="-1">Frozen soil blocks water transport <a class="header-anchor" href="#Frozen-soil-blocks-water-transport" aria-label="Permalink to &quot;Frozen soil blocks water transport {#Frozen-soil-blocks-water-transport}&quot;">​</a></h2><p>Once a layer has a nonzero frozen water content, two independent things stop water moving through it:</p><ul><li><p><strong>Through the soil matrix</strong>: <code>ice_impeded_conductivity</code> scales hydraulic conductivity downward following Bloomsburg and Wang&#39;s (1969) relation, based on the <em>ice-free</em> porosity remaining (<code>porosity - ice_content</code>). Above a threshold (<code>ICE_IMPEDANCE_MIN_POROSITY = 0.13</code> m³/m³) conductivity is reduced roughly in proportion to how much ice-free pore space is left; at or below it, conductivity is floored at a tiny fraction of its unimpeded value (<code>ICE_CONDUCTIVITY_FLOOR_FACTOR = 1e-6</code>, to avoid a literal division/root-finding singularity downstream) bit in practice this results in no meaningful flow.</p></li><li><p><strong>Through roots</strong>: root resistance normally <em>dwarfs</em> soil resistance so the the mechanism above preventing frozen water moving through soil does not prevent it moving through roots. Thus, a layer whose ice-free porosity has crossed the same <code>ICE_IMPEDANCE_MIN_POROSITY</code> threshold is additionally treated as <strong>rootless</strong>: its <code>root_resistance</code> is set prohibitivley high irrespective of that node&#39;s <code>root_density</code>. See <a href="/Microclimate.jl/previews/PR130/manual/soil_moisture#Initialisation">Soil moisture</a> for how <code>root_resistance</code> normally factors into water uptake.</p></li></ul><h2 id="References" tabindex="-1">References <a class="header-anchor" href="#References" aria-label="Permalink to &quot;References {#References}&quot;">​</a></h2><p>See the <a href="./references">References</a> page for the full bibliography.</p>',13)])])}const u=a(r,[["render",n]]);export{m as __pageData,u as default};
